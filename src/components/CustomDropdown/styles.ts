import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 8,
    marginBottom: 20,
  },
  label: {
    fontSize: 16,
    marginBottom: 8,
  },
  boxStyle: {
    borderRadius: 8,
    borderColor: '#D4D4D4',
  },
  dropdownStyle: {
    borderRadius: 8,
  },
  inputStyle: {
    color: '#000',
    fontSize: 14,
  },
  dropdownTextStyle: {
    color: '#000',
    fontSize: 14,
  },
});

export default styles;