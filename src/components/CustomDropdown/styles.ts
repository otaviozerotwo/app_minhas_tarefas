import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  label: {
    fontSize: 16,
  },
  boxStyle: {
    borderRadius: 8,
    borderColor: '#D4D4D4',
    marginTop: 8,
    marginBottom: 20,
  },
  dropdownStyle: {
    borderRadius: 8,
  },
  inputStyle: {
    color: '#000',
    fontSize: 16,
    fontWeight: '500',
  },
  dropdownTextStyle: {
    color: '#000',
    fontSize: 16,
    fontWeight: '500',
  },
});

export default styles;