import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {},
  input: {
    backgroundColor: '#FFF',
    padding: 10,
    marginTop: 8,
    marginBottom: 20,
    fontSize: 14,
    borderWidth: 1,
    borderColor: '#D4D4D4',
    borderRadius: 7,
  },
});

export default styles;